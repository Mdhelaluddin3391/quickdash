# --- Core Framework ---
Django>=5.0,<5.2
djangorestframework>=3.15.0
djangorestframework-simplejwt>=5.3.0
django-cors-headers>=4.3.0

# --- Database ---
# Docker/Dev ke liye binary version theek hai. Prod ke liye 'psycopg2' use karna better hota hai.
psycopg2-binary>=2.9.0
dj-database-url>=1.0.0

# --- Async & Realtime (WebSockets) ---
channels>=4.0.0
channels-redis>=4.1.0
daphne>=4.0.0

# --- Background Tasks (Celery) ---
celery>=5.3.0
redis>=5.0.0
django-redis>=5.4.0

# --- Integrations ---
razorpay>=1.3.0
twilio>=8.0.0
# Ye missing tha, isliye FCM notifications kaam nahi kar rahe the
firebase-admin>=6.2.0

# --- Google Auth (New) ---
google-auth==2.41.0
google-auth-oauthlib==1.2.0
google-auth-httplib2==0.2.0

# --- GeoSpatial / GIS (Maps & Location) ---
# Windows par iska error aa sakta hai bina binary wheels ke. Docker mein ye perfectly chalega.
Shapely>=1.8.0

# --- Utilities ---
python-decouple>=3.8
python-dotenv>=1.0.0
Pillow>=10.0.0  # Image handling ke liye
requests>=2.31.0
drf-spectacular>=0.27.0  # API Documentation ke liye

# --- Server ---
gunicorn>=21.2.0